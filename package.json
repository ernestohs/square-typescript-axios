{
  "name": "square-typescript-axios",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "author": "PLJNS <me@pljns.com>",
  "license": "MIT",
  "scripts": {
    "generate": "yarn clean; openapi-generator-cli generate -i connect-api-specification/api.json -c config.json -g typescript-axios -t template -o src --skip-validate-spec",
    "build": "yarn generate; cd ./src/; yarn install; cd ..; rm -rf dist; tsc -p ./src/tsconfig.json --outDir dist/; yarn clean;",
    "clean": "rm -rf src;",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "axios": "^0.21.0"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.0.3",
    "@types/node": "^14.14.6",
    "typescript": "^4.0.5"
  }
}
